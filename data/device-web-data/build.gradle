android {
    namespace "${rootProject.ext.namespace}.data.device.web"
}

dependencies {
    // application layer
    implementation project(":application:device-application")

    // core layer
    implementation project(":core:data-core:retrofit-data-core")
}