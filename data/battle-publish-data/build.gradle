android {
    namespace "${rootProject.ext.namespace}.data.battle.publish"
}

dependencies {
    // application layer
    implementation project(":application:battle-application")

    // core layer
    implementation project(":core:data-core:mqtt-data-core")
}
