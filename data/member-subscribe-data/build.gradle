android {
    namespace "${rootProject.ext.namespace}.data.member.subscribe"
}

dependencies {
    // application layer
    implementation project(":application:member-application")

    // core layer
    implementation project(":core:data-core:mqtt-data-core")
}
