import org.jetbrains.kotlin.gradle.dsl.JvmTarget

buildscript {
    ext {
    }
}

subprojects {
    apply plugin: "com.android.library"
    apply plugin: "com.google.devtools.ksp"
    apply plugin: "dagger.hilt.android.plugin"

    android {
        compileOptions {
            sourceCompatibility rootProject.ext.javaVersion
            targetCompatibility rootProject.ext.javaVersion
        }

    }

    kotlin {
        compilerOptions {
            jvmTarget = JvmTarget.fromTarget(rootProject.ext.javaVersion.toString())
        }
    }

    dependencies {
        // core layer
        implementation project(":core:presentation-core")

        // 백그라운드/앰비언트에서 Flow 수집을 멈추기 위한 collectAsStateWithLifecycle
        api "androidx.lifecycle:lifecycle-runtime-compose:${rootProject.ext.lifecycleVersion}"

        // dagger & hilt
        implementation group: "javax.inject", name: "javax.inject", version: "1"
        implementation "com.google.dagger:hilt-android:${rootProject.ext.hiltVersion}"
        ksp "com.google.dagger:dagger-compiler:${rootProject.ext.hiltVersion}"
        ksp "com.google.dagger:hilt-compiler:${rootProject.ext.hiltVersion}"
    }
}