apply plugin: "com.android.library"
apply plugin: "org.jetbrains.kotlin.android"
apply plugin: 'com.google.devtools.ksp'

android {
    namespace "${rootProject.ext.namespace}.core.data.store"
}

dependencies {
    // data store
    api "androidx.datastore:datastore-preferences:${parent.ext.dataStoreVersion}"
    api "androidx.datastore:datastore-preferences-core:${parent.ext.dataStoreVersion}"
}